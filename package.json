{
  "name": "wispbox",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "serve -s public",
    "start:watch": "npm run build && npm-run-all --parallel start 'build -- -w'",
    "build": "rollup -c",
    "clean": "rm public/client.js* public/style.css*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rollup": "^1.16.3",
    "rollup-plugin-node-resolve": "^5.2.0",
    "rollup-plugin-svelte": "^5.1.0",
    "serve": "^11.0.2",
    "svelte": "^3.6.2"
  }
}
